1)UPDATE USER SET registration = SUBSTR(registration, 7, 4) || '-' || SUBSTR(registration, 4, 2) || '-' ||
SUBSTR(registration, 1, 2);

2)SELECT login FROM USER order by registration DESC LIMIT 1;

3)SELECT DISTINCT substr(birthday,7,4) FROM USER;

4)SELECT sum(count) FROM PRODUCT;

5)----
